{
  "name": "@elizaos/tardigrade",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Tardigrade host adapter for elizaOS: runs the edge AgentRuntime turn inside a durable Tardigrade actor with journaled model, action, and delivery boundaries.",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./cloudflare": "./src/cloudflare.ts",
    "./actor": "./actor.ts",
    "./package.json": "./package.json"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "bunx @biomejs/biome check --write src tests scripts actor.ts worker.ts server.ts",
    "lint:check": "bunx @biomejs/biome check src tests scripts actor.ts worker.ts server.ts",
    "format": "bunx @biomejs/biome format --write src tests scripts actor.ts worker.ts server.ts",
    "format:check": "bunx @biomejs/biome format src tests scripts actor.ts worker.ts server.ts",
    "test": "bun test tests",
    "build:actor": "bun scripts/tdg.mjs build actor.ts",
    "dev": "bun --env-file=.dev.vars server.ts",
    "dev:cloudflare": "wrangler dev",
    "deploy:cloudflare": "bun scripts/deploy.mjs",
    "smoke:workerd": "bun scripts/smoke.mjs workerd",
    "smoke:deployed": "bun scripts/smoke.mjs deployed",
    "lint:actor": "bun scripts/tdg.mjs lint actor.ts"
  },
  "dependencies": {
    "@effect/platform-bun": "4.0.0-rc.110",
    "@elizaos/core": "workspace:*",
    "@elizaos/plugin-todos": "workspace:*",
    "@elizaos/plugin-web-search": "workspace:*",
    "effect": "4.0.0-rc.110",
    "tardie": "0.25.0"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.8",
    "@cloudflare/workers-types": "^5.20260911.1",
    "@types/node": "^24.0.0",
    "@typescript/native": "npm:typescript@^7.0.2",
    "typescript": "^6.0.3",
    "wrangler": "^4.0.0"
  },
  "elizaos": {
    "scripts": {
      "testLanes": [
        "server"
      ]
    }
  }
}
